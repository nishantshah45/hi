import React from "react";
import Layout from "../components/shared/Layout/Layout";

const Settings = () => {
  return (
    <Layout>
      <div className="container">
        <h1>Settings</h1>
        <p>Configure blood groups, locations, and templates here (placeholder).</p>
      </div>
    </Layout>
  );
};

export default Settings;
